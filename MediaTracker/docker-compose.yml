version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: mediatracker-mysql
    restart: always
    environment:
      MYSQL_DATABASE: mediatracker_db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"